#include <isostream>
#include <cmath>

using namespace std;

int main() {
  double n; 
  cout << "Enter a positive interger: ";
  cin >> n;
    if (n < 0)
       cout << "Number invalid, please enter a POSITIVE integer.\n";
    else 
       cout << n << "Factorial is: " << factorial(n) << endl;
}

  int factorial(double n) {
  int multiplicant=1;

for (int i=1; i<=n; i++)
 {
 
   if (n<=50)
   
   {
    multiplicant=multiplicant*i;
   }
 
 return (1)
 }
 
  if (n>50)
  {
    (n*(log n)) - (n + 0.5*(log(2*PI*n)));
  }
  
}
